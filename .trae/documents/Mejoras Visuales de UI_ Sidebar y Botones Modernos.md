Voy a realizar las siguientes mejoras visuales en tu proyecto Scrum:

1. **Mejorar la presentación del "Proceso del Aprendiz" (Sidebar Colapsado):**

   * Actualmente, la sección de estado del usuario se oculta casi por completo cuando el menú se cierra.

   * **Solución:** Modificaré `css/main.css` para que, al colapsar el menú, se muestre un icono de "Trofeo" o "Nivel" elegante y centrado, manteniendo la visibilidad del progreso de forma compacta (ej. un punto de estado o barra mini).

2. **Modernizar Botones y Estilos Generales (Simuladores y Juegos):**

   * Actualizaré los estilos en `css/components.css` y `css/main.css`.

   * **Botones (`.action-btn`,** **`.nav-item`):** Aplicaré gradientes modernos, bordes redondeados más suaves, y efectos de sombra (`box-shadow`) que reaccionan al pasar el mouse (efecto "elevación").

   * **Contenedores (`.simulator-onboarding`,** **`.sprint-status`):** Reforzaré el efecto "Glassmorphism" con bordes más definidos y sombras más profundas para que destaquen sobre el fondo oscuro.

**Archivos a modificar:**

* `css/main.css`

* `css/components.css`

